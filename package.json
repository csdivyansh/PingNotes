{
  "dependencies": {
    "aos": "^2.3.4",
    "axios": "^1.10.0",
    "docx": "^9.5.1",
    "google-auth-library": "^10.1.0",
    "react-icons": "^5.5.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
